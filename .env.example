# Docker Compose Environment Variables Template
# Copy this file to .env and update the values as needed

# ===================================
# Database Configuration
# ===================================
# PostgreSQL username
POSTGRES_USER=postgres

# PostgreSQL password (change in production!)
POSTGRES_PASSWORD=postgres

# PostgreSQL database name
POSTGRES_DB=data-set-explorer-db

# Database host (use 'database' for docker-compose service name)
DATABASE_HOST=database

# Database port (default PostgreSQL port)
DATABASE_PORT=5432

# ===================================
# Git Credentials
# ===================================
# Two authentication methods are supported:
#
# METHOD 1: HTTPS with Personal Access Token (Recommended for LOCAL DEVELOPMENT)
# ---------------------------------------------------------------------------------
# - Set GIT_USER and GIT_TOKEN below
# - Easier for local development and testing
# GitHub/GitLab username for cloning private repositories
GIT_USER=

# GitHub Personal Access Token or GitLab Token
GIT_TOKEN=

# METHOD 2: SSH with Deploy Key (Recommended for PRODUCTION)
# ---------------------------------------------------------------------------------
# - Use SSH URLs in your Excel import file: git@github.com:owner/repo.git/tree/commit-hash
# - No GIT_USER or GIT_TOKEN needed
# - More secure for production deployments
# - SSH key added to your GitHub account works for ALL repositories you have access to
#
# Setup instructions:
# 1. Generate SSH key: ssh-keygen -t ed25519 -C "dataset-explorer" -f ./deploy_key
# 2. Copy ssh_config.example to ssh_config: cp ssh_config.example ssh_config
# 3. Add deploy_key.pub to GitHub: Profile → Settings → SSH and GPG keys → New SSH key
# 4. Use docker-compose.prod.yml: docker-compose -f docker-compose.prod.yml up -d --build
#
# Note: For production, leave GIT_USER and GIT_TOKEN empty when using SSH

# ===================================
# Frontend Configuration
# ===================================
# Port to expose frontend on the host machine
# Default: 4200
# Change if port 4200 is already in use
FRONTEND_PORT=4200

# Path to the frontend application source code
# This is required if your frontend code is located outside the main project directory
# Use forward slashes (/) even on Windows
# Example: D:/projects/platform-explorer-ui-web
# Default: ../platform-explorer-ui-web (assumes frontend is in parent directory)
FRONTEND_PATH=../platform-explorer-ui-web

# ===================================
# Export Configuration
# ===================================
# Path to user's Downloads folder where exported files will be saved
# Use forward slashes (/) even on Windows
# Example: C:/Users/YourUsername/Downloads
DOWNLOADS_PATH=C:/Users/YourUsername/Downloads
